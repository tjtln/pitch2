{
  "name": "websocket",
  "version": "1.0.0",
  "description": "14 point pitch game!",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && cp package.json dist/ && cp package-lock.json dist/ && cd dist && npm install"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^24.1.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "aws-sdk": "^2.1692.0"
  }
}
